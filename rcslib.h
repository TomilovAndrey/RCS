#ifndef _RCSLIB_h
#define _RCSLIB_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "wprogram.h"
#else
	#include "WProgram.h"
#endif

#include <Motions\motions.h>
#include <Parse\parse.h>
#endif